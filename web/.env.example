# Optional fixed mount path such as /studio. Leave empty to infer it at runtime
# from the loaded asset URL (recommended for temporary path-prefix proxies).
VITE_APP_BASE_PATH=
# Optional API path/origin. Leave empty to use the application mount path.
VITE_API_BASE=
